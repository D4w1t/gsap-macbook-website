/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/gsap-macbook-website",
  output: "export", // <=== enables static exports
  reactStrictMode: true,
};

module.exports = nextConfig;
